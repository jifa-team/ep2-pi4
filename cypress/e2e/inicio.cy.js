describe('Configuração Inicial do Cypress', () => {
  it('Passa neste teste de exemplo para validar o pipeline de CI/CD', () => {
    // Teste simples garante que o Cypress execute e termine com sucesso.
    expect(true).to.equal(true);
  });
});